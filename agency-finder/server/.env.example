DATABASE_URL=
GOOGLE_MAPS_API_KEY=
PORT=3001
NODE_ENV=development
# Local Vite origin. When API serves the SPA (production Docker), omit or match your site URL if needed.
CORS_ORIGIN=http://localhost:5173
# Optional: absolute path to client dist (Railway Dockerfile uses default layout under /app/client/dist).
# CLIENT_DIST_PATH=
